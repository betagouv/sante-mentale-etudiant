import { createModal } from "@codegouvfr/react-dsfr/Modal";

export const needHelpModal = createModal({
  id: "need-help-modal",
  isOpenedByDefault: false,
});

export const sseModal = createModal({
  id: "sse-modal",
  isOpenedByDefault: false,
});
